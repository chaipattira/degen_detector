# Core dependencies for degen_detector
numpy>=1.20.0
scipy>=1.7.0
sympy>=1.9
pysr>=0.16.0
matplotlib>=3.3.0

# For corner plots (optional but used in plotting.py)
corner>=2.2.0

# Optional chain format loaders
getdist>=1.7
emcee>=3.1
h5py>=3.0

# Optional SBI/ML dependencies (for CAMELS SB28 example)
# Install in order (ltu-ili not on PyPI):
#   uv pip install torch sbi lampe zuko --project /home/x-ctirapongpra/scratch/degen_detector
#   uv pip install /anvil/scratch/x-ctirapongpra/ltu-ili --project /home/x-ctirapongpra/scratch/degen_detector

# Testing dependencies
pytest>=7.0.0
pytest-cov>=3.0.0
